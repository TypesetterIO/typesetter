# Introduction

This is the first chapter of our demo book. The `FirstElementInChapterCSSClass`
observer skips the very first chapter by default, so this paragraph should
*not* receive the `chapter-beginning` class.

## A short section

Typesetter takes Markdown and produces a PDF using MPDF, with observers that
hook into the rendering lifecycle.

Here is a fenced code block to exercise the default markdown configuration:

```php
<?php

echo "Hello from Typesetter.io";
```

That's the end of chapter one.
